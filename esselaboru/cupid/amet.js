// If multiple data attributes are accessed, cache the dataset object
const data = targetElement.dataset;
const action = data.clickAction;

// Use the action directly in your logic
// Example: if(action === 'someAction') { ... }
